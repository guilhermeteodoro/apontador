.fluid-content
  #message= flash_message
  %h1.center #{@current_user.name}
  / %p.muted.center
  /   %b clique em check-in para iniciar a contagem

  %br
  #clock
  %br

  .center
    = form_for @checking do |f|
      - render partial: 'form', locals: { f: f, button_name: "Check-in" }

:javascript
  startClock();